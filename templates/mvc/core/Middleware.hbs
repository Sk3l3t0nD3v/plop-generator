<?php



namespace app\core;

use app\exceptions\AuthException;
use app\models\User;

abstract class Middleware
{
    function __construct()
    {
    }

    abstract public function execute(User|null $user): User|AuthException;
}
