<?php

namespace app\exceptions;

use Exception;

class AuthException extends Exception
{
    protected $message;

    function __construct(string $message)
    {
        $this->message =  json_encode(['error' => true, 'message' => $message]);
    }
}
